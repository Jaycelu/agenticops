# AgenticOps UI 重构实施计划

1. 重写全局设计令牌和共享组件样式，移除渐变、玻璃模糊、普遍阴影和过大圆角。
2. 重构 `Layout.vue` 与 `TopNav.vue`，建立 232px 桌面侧边栏和响应式顶部导航。
3. 重构 Dashboard 的信息层级、KPI 带、状态列表和枚举显示，不改变数据加载逻辑。
4. 统一登录页与控制台的品牌语言、表单焦点态和错误状态。
5. 扫描其余页面对共享类的使用，修复因全局令牌变化导致的布局或对比度问题。
6. 运行前端构建和测试；启动本地服务进行多视口截图检查。
