[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    unit: fast test without external services
    integration: test requiring a real PostgreSQL service
